{
  "name": "podcast-pwa",
  "private": true,
  "version": "1.0.0",
  "description": "個人利用向けポッドキャスト再生PWA（ビルド不要・静的ホスティング）",
  "scripts": {
    "start": "npx --yes http-server . -p 8080 -c-1",
    "test": "node test/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
